import React, { Component } from 'react';
import Header from './containers/Header';

class Home extends Component {
    render() {
        return (
            <Header></Header>

        );
    }
};

export default Home;